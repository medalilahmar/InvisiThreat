name: DevSecOps Pipeline - InvisiThreat (Optimisé)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

# ============================================
# JOB 1 : SAST (Semgrep) - 5 minutes max
# ============================================
jobs:
  sast:
    name: 🔍 SAST - Semgrep
    runs-on: self-hosted
    timeout-minutes: 10
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check Docker images
        run: |
          if ! docker image inspect returntocorp/semgrep >/dev/null 2>&1; then
            echo "📦 Image Semgrep non trouvée, téléchargement..."
            docker pull returntocorp/semgrep:latest
          else
            echo "✅ Image Semgrep déjà présente"
          fi

      - name: SAST with Semgrep
        working-directory: ./juice-shop
        continue-on-error: true
        run: |
          echo "🔍 Scan Semgrep en cours..."
          START_TIME=$(date +%s)
          
          docker run --rm -v $(pwd):/src returntocorp/semgrep semgrep \
            --config=p/security-audit \
            --config=p/owasp-top-ten \
            --json \
            --output=semgrep-results.json \
            /src || true
          
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          
          if [ ! -f "semgrep-results.json" ]; then
            echo '{"results":[]}' > semgrep-results.json
            echo "⚠️ Fichier semgrep-results.json créé (vide)"
          else
            COUNT=$(jq '.results | length' semgrep-results.json 2>/dev/null || echo "0")
            echo "✅ Semgrep terminé en ${DURATION}s : $COUNT résultats"
          fi

      - name: Upload SAST results
        uses: actions/upload-artifact@v4
        with:
          name: sast-results
          path: juice-shop/semgrep-results.json
          retention-days: 30
          if-no-files-found: warn

# ============================================
# JOB 2 : SCA (Snyk) - 5 minutes max
# ============================================
  sca:
    name: 📦 SCA - Snyk
    runs-on: self-hosted
    timeout-minutes: 10
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: |
            juice-shop/node_modules
            ~/.npm
          key: ${{ runner.os }}-modules-${{ hashFiles('juice-shop/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-modules-

      - name: Install dependencies (ultra rapide)
        working-directory: ./juice-shop
        run: |
          # Vérification intelligente des dépendances
          if [ ! -d "node_modules" ] || [ ! -f "node_modules/.lock-hash" ]; then
            echo "📦 Première installation..."
            npm ci --prefer-offline --no-audit --no-fund
            md5sum package-lock.json > node_modules/.lock-hash
          else
            # Vérifier si package-lock.json a changé
            CURRENT_HASH=$(md5sum package-lock.json | cut -d' ' -f1)
            SAVED_HASH=$(cut -d' ' -f1 node_modules/.lock-hash 2>/dev/null || echo "")
            
            if [ "$CURRENT_HASH" != "$SAVED_HASH" ]; then
              echo "📦 package-lock.json modifié, mise à jour..."
              npm ci --prefer-offline --no-audit --no-fund
              md5sum package-lock.json > node_modules/.lock-hash
            else
              echo "✅ Dépendances déjà à jour"
            fi
          fi
          
          # Vérifier que tout fonctionne
          npm list --depth=0 >/dev/null 2>&1 || npm install --no-package-lock

      - name: SCA with Snyk (ultra rapide)
        continue-on-error: true
        run: |
          cd juice-shop
          echo "🔍 Scan Snyk en cours..."
          START_TIME=$(date +%s)
          
          # Utiliser snyk global s'il existe, sinon npx
          if command -v snyk &> /dev/null; then
            SNYK_CMD="snyk"
          else
            # Installer Snyk globalement sur le runner (une seule fois)
            npm install -g snyk
            SNYK_CMD="snyk"
          fi
          
          $SNYK_CMD test --severity-threshold=low \
            --json-file-output=snyk-results.json || true
          
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          
          if [ ! -f "snyk-results.json" ] || [ ! -s "snyk-results.json" ]; then
            echo '{"vulnerabilities":[]}' > snyk-results.json
            echo "⚠️ Fichier snyk-results.json créé (vide)"
          else
            COUNT=$(jq '.vulnerabilities | length' snyk-results.json 2>/dev/null || echo "0")
            echo "✅ Snyk terminé en ${DURATION}s : $COUNT vulnérabilités"
          fi
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Snyk Monitor
        continue-on-error: true
        run: |
          cd juice-shop
          if command -v snyk &> /dev/null; then
            snyk monitor || true
          else
            npx snyk monitor || true
          fi
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Upload SCA results
        uses: actions/upload-artifact@v4
        with:
          name: sca-results
          path: juice-shop/snyk-results.json
          retention-days: 30
          if-no-files-found: warn

# ============================================
# JOB 3 : DAST (ZAP) - 30 minutes max
# ============================================
  dast:
    name: 🌐 DAST - ZAP
    runs-on: self-hosted
    timeout-minutes: 40
    needs: [sast, sca]  # Démarre après SAST/SCA
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: juice-shop/node_modules
          key: ${{ runner.os }}-modules-${{ hashFiles('juice-shop/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-modules-

      - name: Install dependencies (léger)
        working-directory: ./juice-shop
        run: |
          if [ ! -d "node_modules" ]; then
            npm ci --prefer-offline --no-audit --no-fund --production
          else
            echo "✅ Dépendances déjà présentes"
          fi

      - name: Check ZAP Docker image
        run: |
          if ! docker image inspect ghcr.io/zaproxy/zaproxy:stable >/dev/null 2>&1; then
            echo "📦 Image ZAP non trouvée, téléchargement..."
            docker pull ghcr.io/zaproxy/zaproxy:stable
          else
            echo "✅ Image ZAP déjà présente"
          fi

      - name: Start Juice Shop
        working-directory: ./juice-shop
        run: |
          # Nettoyage préalable
          pkill -f "node.*juice-shop" 2>/dev/null || true
          sleep 3
          
          echo "🚀 Démarrage de Juice Shop..."
          npm start &
          echo $! > api.pid
          
          # Health check amélioré
          MAX_RETRIES=20
          for i in $(seq 1 $MAX_RETRIES); do
            if curl -s -f http://localhost:3000/ > /dev/null 2>&1; then
              echo "✅ API prête (tentative $i/$MAX_RETRIES)"
              exit 0
            fi
            echo "⏳ Attente... ($i/$MAX_RETRIES)"
            sleep 3
          done
          
          echo "❌ Échec du démarrage"
          exit 1

      - name: DAST - OpenAPI Scan
        continue-on-error: true
        working-directory: ./juice-shop
        run: |
          echo "🕷️ Scan OpenAPI avec ZAP..."
          
          # Détection du fichier OpenAPI
          API_FILE=""
          for file in swagger.yml swagger.yaml openapi.json swagger.json; do
            if [ -f "$file" ]; then
              API_FILE="$file"
              echo "📄 Fichier trouvé: $file"
              break
            fi
          done
          
          # Scan avec timeout optimisé
          if [ -n "$API_FILE" ]; then
            docker run --rm --network host -v $(pwd):/zap/wrk:rw \
              ghcr.io/zaproxy/zaproxy:stable \
              zap-api-scan.py -t http://localhost:3000 -f openapi \
              -d /zap/wrk/$API_FILE \
              -x /zap/wrk/zap-openapi.xml -J /zap/wrk/zap-openapi.json -T 5 || true
          else
            docker run --rm --network host -v $(pwd):/zap/wrk:rw \
              ghcr.io/zaproxy/zaproxy:stable \
              zap-api-scan.py -t http://localhost:3000 -f openapi \
              -x /zap/wrk/zap-openapi.xml -J /zap/wrk/zap-openapi.json -T 5 || true
          fi
          
          # Analyse rapide des résultats
          if [ -f "zap-openapi.json" ] && [ -s "zap-openapi.json" ]; then
            ALERTS=$(jq '.site[0].alerts | length' zap-openapi.json 2>/dev/null || echo "0")
            echo "✅ Scan OpenAPI: $ALERTS alertes"
          fi

      - name: DAST - Active Scan (optimisé)
        continue-on-error: true
        timeout-minutes: 20
        run: |
          echo "⚔️ Scan actif ZAP (15-20 minutes)..."
          
          docker run --rm --network host -v $(pwd)/juice-shop:/zap/wrk:rw \
            ghcr.io/zaproxy/zaproxy:stable \
            zap-full-scan.py -t http://localhost:3000 \
            -x /zap/wrk/zap-active.xml -J /zap/wrk/zap-active.json \
            -T 15 \  # Timeout 15 minutes
            -m 5 \   # Max attaques/minute
            -z "-config scanner.attackStrength=HIGH" \
            -z "-config scanner.threadPerHost=5" || true
          
          if [ -f "juice-shop/zap-active.json" ] && [ -s "juice-shop/zap-active.json" ]; then
            ALERTS=$(jq '.site[0].alerts | length' juice-shop/zap-active.json 2>/dev/null || echo "0")
            echo "✅ Scan actif: $ALERTS alertes"
          fi

      - name: Stop Juice Shop
        if: always()
        working-directory: ./juice-shop
        run: |
          if [ -f api.pid ]; then
            kill $(cat api.pid) 2>/dev/null || true
            rm -f api.pid
          fi
          pkill -f "node.*juice-shop" 2>/dev/null || true

      - name: Upload DAST results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dast-results
          path: |
            juice-shop/zap-*.json
            juice-shop/zap-*.xml
          retention-days: 30
          if-no-files-found: warn

# ============================================
# JOB 4 : Consolidation et Import
# ============================================
  import:
    name: 📊 Consolidation & Import
    runs-on: self-hosted
    needs: [sast, sca, dast]
    timeout-minutes: 5
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Consolidate results
        run: |
          echo "📊 Consolidation des résultats..."
          mkdir -p juice-shop security-summary
          
          # Copier tous les résultats dans juice-shop/
          find ./artifacts -name "*.json" -exec cp {} juice-shop/ \; 2>/dev/null || true
          find ./artifacts -name "*.xml" -exec cp {} juice-shop/ \; 2>/dev/null || true
          
          # Analyse Semgrep
          SEMGREP_COUNT=0
          if [ -f "juice-shop/semgrep-results.json" ]; then
            SEMGREP_COUNT=$(jq '.results | length' juice-shop/semgrep-results.json 2>/dev/null || echo "0")
          fi
          
          # Analyse Snyk
          SNYK_COUNT=0
          CRITICAL=0; HIGH=0; MEDIUM=0; LOW=0
          if [ -f "juice-shop/snyk-results.json" ]; then
            SNYK_COUNT=$(jq '.vulnerabilities | length' juice-shop/snyk-results.json 2>/dev/null || echo "0")
            CRITICAL=$(jq '[.vulnerabilities[] | select(.severity=="critical")] | length' juice-shop/snyk-results.json 2>/dev/null || echo "0")
            HIGH=$(jq '[.vulnerabilities[] | select(.severity=="high")] | length' juice-shop/snyk-results.json 2>/dev/null || echo "0")
            MEDIUM=$(jq '[.vulnerabilities[] | select(.severity=="medium")] | length' juice-shop/snyk-results.json 2>/dev/null || echo "0")
            LOW=$(jq '[.vulnerabilities[] | select(.severity=="low")] | length' juice-shop/snyk-results.json 2>/dev/null || echo "0")
          fi
          
          # Analyse ZAP
          ZAP_COUNT=0
          if [ -f "juice-shop/zap-active.json" ]; then
            ZAP_COUNT=$(jq '.site[0].alerts | length' juice-shop/zap-active.json 2>/dev/null || echo "0")
          fi
          
          # Résumé
          {
            echo "========================================"
            echo "    RAPPORT DE SÉCURITÉ - InvisiThreat"
            echo "========================================"
            echo "🔍 SAST (Semgrep): $SEMGREP_COUNT résultats"
            echo "📦 SCA (Snyk): $SNYK_COUNT vulnérabilités"
            echo "   → Critiques: $CRITICAL | Hautes: $HIGH | Moyennes: $MEDIUM | Basses: $LOW"
            echo "🌐 DAST (ZAP): $ZAP_COUNT alertes"
            echo "========================================"
            echo "Commit: ${{ github.sha }}"
            echo "Branche: ${{ github.ref_name }}"
            echo "Date: $(date)"
            echo "========================================"
          } > security-summary/summary.txt
          
          cat security-summary/summary.txt

      - name: Import to DefectDojo
        continue-on-error: true
        env:
          DD_URL: ${{ secrets.DD_URL }}
          DD_API_KEY: ${{ secrets.DD_API_KEY }}
          PRODUCT_ID: ${{ secrets.PRODUCT_ID }}
        run: |
          if [ -z "$DD_URL" ] || [ -z "$DD_API_KEY" ] || [ -z "$PRODUCT_ID" ]; then
            echo "⚠️ DefectDojo non configuré"
            exit 0
          fi
          
          echo "📤 Import dans DefectDojo..."
          
          # Création d'un engagement
          ENGAGEMENT_NAME="Scan-$(date +%Y%m%d-%H%M%S)-${{ github.sha }}"
          echo "Création engagement: $ENGAGEMENT_NAME"
          
          ENGAGEMENT_RESPONSE=$(curl -s -X POST "$DD_URL/api/v2/engagements/" \
            -H "Authorization: Token $DD_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{
              "name": "'"$ENGAGEMENT_NAME"'",
              "product": '"$PRODUCT_ID"',
              "target_start": "'$(date +%Y-%m-%d)'",
              "target_end": "'$(date -d "+30 days" +%Y-%m-%d)'",
              "status": "In Progress"
            }')
          
          ENGAGEMENT_ID=$(echo "$ENGAGEMENT_RESPONSE" | jq -r '.id // empty')
          
          if [ -z "$ENGAGEMENT_ID" ] || [ "$ENGAGEMENT_ID" = "null" ]; then
            echo "❌ Échec création engagement"
            exit 0
          fi
          
          echo "✅ Engagement #$ENGAGEMENT_ID créé"
          
          # Imports
          for file in juice-shop/*.json; do
            if [ -f "$file" ] && [ -s "$file" ]; then
              case "$file" in
                *semgrep*) SCAN_TYPE="Semgrep JSON Report" ;;
                *snyk*) SCAN_TYPE="Snyk Scan" ;;
                *zap*) SCAN_TYPE="ZAP Scan" ;;
                *) continue ;;
              esac
              
              echo "📤 Import $SCAN_TYPE..."
              curl -s -X POST "$DD_URL/api/v2/import-scan/" \
                -H "Authorization: Token $DD_API_KEY" \
                -F "engagement=$ENGAGEMENT_ID" \
                -F "scan_type=$SCAN_TYPE" \
                -F "file=@$file" \
                -F "close_old_findings=true" \
                -F "minimum_severity=Info" > /dev/null
            fi
          done
          
          for file in juice-shop/*.xml; do
            if [ -f "$file" ] && [ -s "$file" ]; then
              echo "📤 Import ZAP XML..."
              curl -s -X POST "$DD_URL/api/v2/import-scan/" \
                -H "Authorization: Token $DD_API_KEY" \
                -F "engagement=$ENGAGEMENT_ID" \
                -F "scan_type=ZAP Scan" \
                -F "file=@$file" \
                -F "close_old_findings=true" > /dev/null
            fi
          done
          
          echo "✅ Import terminé"

      - name: Upload consolidated summary
        uses: actions/upload-artifact@v4
        with:
          name: security-summary
          path: security-summary/
          retention-days: 30