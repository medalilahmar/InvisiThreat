# This directory intentionally NOT in .gitignore
# to simulate credentials committed to source control.
# SECURITY MISCONFIGURATION: .env with real credentials committed.

Uploaded files will appear here and be publicly accessible via:
  http://localhost:5000/uploads/<filename>

Directory listing is ENABLED via serve-index.
Files of ALL types are accepted (PHP, HTML, SH, EXE, etc.)
